function merge(obj, ...sources) {
  // if (typeof obj !== 'object') {
  //   throw new Error('Enter an object as the first parameter');
  // }

  // for (let key in sources[1]) {
  //   if () {
  //   }
  // }
  
  // return obj;
}

export default merge;
